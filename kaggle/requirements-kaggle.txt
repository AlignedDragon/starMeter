# Extra installs for the self-contained Kaggle notebook, on top of Kaggle's
# preinstalled torch / opencv / pycocotools / scipy. The notebook trains a PLAIN
# Mask2Former (no denoising decoder hooks), so transformers is not version-pinned
# — any recent release with the Mask2Former image processor works.
transformers>=4.45
pycocotools>=2.0.7
scipy>=1.10
